import React from 'react'
import { HeroText } from './aurora-background'

export default function Hero() {
  return (
    <div className='w-full flex  items-center justify-between  h-full min-h-screen'>
      <HeroText />
    </div>
  )
}
